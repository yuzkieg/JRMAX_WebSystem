This operation replaced the navigation <nav> block in admin blade files to include:
class="... {{ request()->is(ltrim($item['url'], '/') . '*') ? 'bg-red-600/60 translate-x-2' : 'hover:bg-red-600/40 hover:translate-x-2' }}"
Use request()->is so routes with subpaths match. Keep this pattern for future modules.